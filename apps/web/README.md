# Web App (Phase 1 Foundation)

This app contains the initial Phase 1 baseline for Project Shop 1.

## Run locally
1. `cp .env.example .env.local`
2. `npm install`
3. `npm run dev`

## Validate
- `npm run lint`
- `npm run build`
